apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: auth-ingress
spec:
  rules:
  - host: auth.${DOMAIN}
    http:
      paths:
      - path: /auth
        pathType: Prefix
        backend:
          service:
            name: auth-api
            port:
              number: 80

